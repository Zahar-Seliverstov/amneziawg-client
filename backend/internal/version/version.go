// Package version хранит номер сборки.
//
// Единственный источник правды — поле "version" в
// desktop/src-tauri/tauri.conf.json: именно его сверяет с тегом workflow
// релиза. Makefile вычитывает оттуда номер и подставляет сюда на сборке.
//
// Раньше версий было четыре независимых: три манифеста и число, вписанное
// руками в экран настроек. Последнее никто не обновлял, и приложение 1.1.0
// показывало пользователю «1.0.0».
package version

// Value подставляется на сборке: -ldflags "-X .../internal/version.Value=1.2.3".
// Без подстановки остаётся "dev" — сборка из дерева разработки так и
// называется, и выдавать её за выпущенную версию не нужно.
var Value = "dev"
