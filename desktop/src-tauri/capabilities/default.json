{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Окну нужны только события: статус подключения и результат запуска службы. Всё остальное интерфейс делает через команду api_request.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
